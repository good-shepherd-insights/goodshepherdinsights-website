---
enable: true
badge: "Fractional CTO services"
title: "Strategic guidance for every technology decision"
description: "Move past ad-hoc vendor decisions. Get a professional partner to help you build a roadmap that supports your ministry and your stewardship."
image: "/images/services/what-we-prove.png"
imageAlt: "Fractional CTO services"
button:
  # Refer to the `sharedButton` schema in `src/sections.schema.ts` for all available configuration options
  # (e.g., enable, label, url, hoverEffect, variant, icon, tag, rel, class, target, etc.)
  enable: true
  label: "Explore our services"
  url: "/services/"
  variant: "fill" # Optional: fill | outline | text | circle | white
  # hoverEffect: "" # Optional: text-flip | creative-fill | magnetic | magnetic-text-flip
  # rel: "" # Optional
  # target: "" # Optional
cardLayout: "horizontal" # Optional: listImage | horizontal (default)
limit: 4
---
